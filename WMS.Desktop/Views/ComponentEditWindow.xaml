<Window x:Class="WMS.Desktop.Views.ComponentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WMS.Desktop.Views"
        mc:Ignorable="d"
        Title="ComponentEditWindow" 
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#36495b"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Артикул:" VerticalAlignment="Center" Foreground="White" Margin="0,5" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Article, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="25" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Название:" VerticalAlignment="Center" Foreground="White" Margin="0,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="25" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Производитель:" VerticalAlignment="Center" Foreground="White" Margin="0,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Manufacturer, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="25" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Срок годности:" VerticalAlignment="Center" Foreground="White" Margin="0,5"/>
        <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding ExpirationDate, Mode=TwoWay}" Margin="0,5"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Мин. остаток:" VerticalAlignment="Center" Foreground="White" Margin="0,5"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding MinQuantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="25" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Применить" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Width="100" Height="30" Margin="0,0,10,0" Style="{StaticResource ButtonStyle}"/>
            <Button Content="Отменить" IsCancel="True" Width="100" Height="30" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
