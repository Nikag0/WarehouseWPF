<UserControl x:Class="WMS.Desktop.Views.RacksGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WMS.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="19*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="11*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0"
              Grid.Row="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="1" Margin="0,15,0,0" 
                HorizontalAlignment="Center" 
                Style="{StaticResource HeaderTextStyle}"/>

            <TextBlock Grid.Row="1" Text="2" Margin="0,120,0,0" 
                HorizontalAlignment="Center" 
                Style="{StaticResource HeaderTextStyle}"/>

            <TextBlock Grid.Row="2" Text="3" Margin="0,10,0,0" 
                HorizontalAlignment="Center" 
                Style="{StaticResource HeaderTextStyle}"/>

            <TextBlock Grid.Row="3" Text="4" Margin="0,100,0,0" 
                HorizontalAlignment="Center" 
                Style="{StaticResource HeaderTextStyle}"/>

            <TextBlock Grid.Row="4" Text="5" Margin="0,120,0,0" 
                HorizontalAlignment="Center" 
                Style="{StaticResource HeaderTextStyle}"/>

        </Grid>

        <Grid Grid.Column="1"
              Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Style="{StaticResource HeaderTextStyle}" Text="А"/>
            <TextBlock Grid.Column="1" Style="{StaticResource HeaderTextStyle}" Text="Б"/>
            <TextBlock Grid.Column="2" Style="{StaticResource HeaderTextStyle}" Text="В"/>
            <TextBlock Grid.Column="3" Style="{StaticResource HeaderTextStyle}" Text="Г"/>

        </Grid>

        <Viewbox  Grid.Column="1"
                  Grid.Row="1"
                  Stretch="Fill">
            
            <Grid Width="1000" Height="800">

                <Image Source="/Images/Warehouse.png"
                       Stretch="Fill"/>

                <ItemsControl ItemsSource="{Binding Racks, RelativeSource={RelativeSource AncestorType=local:RacksGridControl}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="Canvas.Left" Value="{Binding X}" />
                            <Setter Property="Canvas.Top" Value="{Binding Y}" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="{Binding Width}"
                                    Height="{Binding Height}">

                                <Border.Style>
                                    
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Opacity" Value="5"/>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="BorderBrush" Value="LightBlue"/>
                                                <Setter Property="BorderThickness" Value="10"/>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding ItemInCell}" Value="True">
                                                <Setter Property="Background" Value="#CDA434"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Border.InputBindings>
                                    <MouseBinding Gesture="LeftClick"
                                              Command="{Binding DataContext.SelectRackCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}" />
                                </Border.InputBindings>

                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </Grid>
        </Viewbox>
        
    </Grid>
</UserControl>
