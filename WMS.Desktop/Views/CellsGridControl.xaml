<UserControl x:Class="WMS.Desktop.Views.CellsGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WMS.Desktop.Views"
             xmlns:converters="clr-namespace:WMS.Desktop.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:RackTypeToVisibilityConverter x:Key="RackTypeToVisibilityConverter"/>   
        <converters:RackImageConverter x:Key="RackImageConverter"/>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid MaxHeight="450" MaxWidth="300">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="19*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="11*"/>
        </Grid.RowDefinitions>

        <!-- Header / подписи для разных типов стеллажей -->
        <Grid Grid.Column="1" Grid.Row="0"
              Visibility="{Binding RackType, RelativeSource={RelativeSource AncestorType=UserControl},
                                   Converter={StaticResource RackTypeToVisibilityConverter},
                                   ConverterParameter=RackType1}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Style="{StaticResource HeaderTextStyle}" Text="А"/>
            <TextBlock Grid.Column="1" Style="{StaticResource HeaderTextStyle}" Text="Б"/>
            <TextBlock Grid.Column="2" Style="{StaticResource HeaderTextStyle}" Text="В"/>
            
        </Grid>

        <Grid Grid.Column="1" Grid.Row="0"
              Visibility="{Binding RackType, RelativeSource={RelativeSource AncestorType=UserControl},
                                   Converter={StaticResource RackTypeToVisibilityConverter},
                                   ConverterParameter=RackType2}">
            <!-- Вариант 2 для RackType1 (4 колонки) -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Style="{StaticResource HeaderTextStyle}" Text="А"/>
            <TextBlock Grid.Column="1" Style="{StaticResource HeaderTextStyle}" Text="Б"/>
            <TextBlock Grid.Column="2" Style="{StaticResource HeaderTextStyle}" Text="В"/>
            <TextBlock Grid.Column="3" Style="{StaticResource HeaderTextStyle}" Text="Г"/>
        </Grid>

        <Grid Grid.Column="1" Grid.Row="0"
              Visibility="{Binding RackType, RelativeSource={RelativeSource AncestorType=UserControl},
                                   Converter={StaticResource RackTypeToVisibilityConverter},
                                   ConverterParameter=RackType3}">
            <!-- Вариант для RackType2 -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Style="{StaticResource HeaderTextStyle}" Text="А"/>
        </Grid>

        <!-- левый столбец с номерами рядов (вариант для RackType1) -->
        <Grid Grid.Column="0" Grid.Row="1"
              Visibility="{Binding RackType, RelativeSource={RelativeSource AncestorType=UserControl},
                                   Converter={StaticResource RackTypeToVisibilityConverter},
                                   ConverterParameter=RackType1}">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Style="{StaticResource HeaderTextStyle}" Text="1"/>
            <TextBlock Grid.Row="1" Style="{StaticResource HeaderTextStyle}" Text="2"/>
            <TextBlock Grid.Row="2" Style="{StaticResource HeaderTextStyle}" Text="3"/>
            <TextBlock Grid.Row="3" Style="{StaticResource HeaderTextStyle}" Text="4"/>
            <TextBlock Grid.Row="4" Style="{StaticResource HeaderTextStyle}" Text="5"/>
            <TextBlock Grid.Row="5" Style="{StaticResource HeaderTextStyle}" Text="6"/>
            <TextBlock Grid.Row="6" Style="{StaticResource HeaderTextStyle}" Text="7"/>
            
        </Grid>

        <!-- левый столбец номеров для RackType2 -->
        <Grid Grid.Column="0" Grid.Row="1"
              Visibility="{Binding RackType, RelativeSource={RelativeSource AncestorType=UserControl},
                                   Converter={StaticResource RackTypeToVisibilityConverter},
                                   ConverterParameter=RackType2}">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Style="{StaticResource HeaderTextStyle}" Text="1"/>
            <TextBlock Grid.Row="1" Style="{StaticResource HeaderTextStyle}" Text="2"/>
            <TextBlock Grid.Row="2" Style="{StaticResource HeaderTextStyle}" Text="3"/>
            <TextBlock Grid.Row="3" Style="{StaticResource HeaderTextStyle}" Text="4"/>
            <TextBlock Grid.Row="4" Style="{StaticResource HeaderTextStyle}" Text="5"/>
            <TextBlock Grid.Row="5" Style="{StaticResource HeaderTextStyle}" Text="6"/>
            <TextBlock Grid.Row="6" Style="{StaticResource HeaderTextStyle}" Text="7"/>
        </Grid>

        <!-- левый столбец номеров для RackType3 -->
        <Grid Grid.Column="0" Grid.Row="1"
              Visibility="{Binding RackType, RelativeSource={RelativeSource AncestorType=UserControl},
                                   Converter={StaticResource RackTypeToVisibilityConverter},
                                   ConverterParameter=RackType3}">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Style="{StaticResource HeaderTextStyle}" Text="1"/>
            <TextBlock Grid.Row="1" Style="{StaticResource HeaderTextStyle}" Text="2"/>
            <TextBlock Grid.Row="2" Style="{StaticResource HeaderTextStyle}" Text="3"/>
            <TextBlock Grid.Row="3" Style="{StaticResource HeaderTextStyle}" Text="4"/>
            <TextBlock Grid.Row="4" Style="{StaticResource HeaderTextStyle}" Text="5"/>
            <TextBlock Grid.Row="5" Style="{StaticResource HeaderTextStyle}" Text="6"/>
            <TextBlock Grid.Row="6" Style="{StaticResource HeaderTextStyle}" Text="7"/>
        </Grid>

        <Viewbox Grid.Column="1"
                 Grid.Row="1"
                 Stretch="Fill">
            <Grid>

                <Image Source="{Binding SelectedRack.Type, Converter={StaticResource RackImageConverter}}"
                       Stretch="Fill"/>

                <ItemsControl ItemsSource="{Binding Cells, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="Canvas.Left" Value="{Binding X}" />
                            <Setter Property="Canvas.Top" Value="{Binding Y}" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="{Binding Width}"
                                    Height="{Binding Height}"
                                    BorderBrush="Black"
                                    BorderThickness="1"
                                    Opacity="0.5">
                                
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="Red"/>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                                <Setter Property="Background" Value="Red"/>
                                            </DataTrigger>
                                            
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Border.InputBindings>
                                    <MouseBinding Gesture="LeftClick"
                                      Command="{Binding DataContext.SelectCellCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                      CommandParameter="{Binding}" />
                                </Border.InputBindings>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </Grid>
        </Viewbox>
    </Grid>
</UserControl>