<UserControl x:Class="WMS.Desktop.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WMS.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="10,0"
       Background="#36495b">

        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="История операций"
                   Style="{StaticResource WindowNames}"
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="1"
                 Margin="0,0,0,10" 
                 Tag="Поиск:"
                 Style="{StaticResource SearchTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

        <Grid Grid.Row="2"
              Width="300"
              HorizontalAlignment="Left"
              Margin="0,0,0,10">
        </Grid>

        <Grid Grid.Row="3"
              Margin="0,0,0,10">

            <DataGrid ItemsSource="{Binding HistoryItems}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      FontSize="15"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True">
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата" Binding="{Binding OccurredAt}" Width="100*"/>
                    <DataGridTextColumn Header="Оператор" Binding="{Binding Operator}" Width="200*"/>
                    <DataGridTextColumn Header="Тип операции" Binding="{Binding OperationType}" Width="100*"/>
                    <DataGridTextColumn Header="Компонент" Binding="{Binding ComponentName}" Width="100*"/>
                    <DataGridTextColumn Header="Кол-во до" Binding="{Binding ComponentName}" Width="100*"/>
                    <DataGridTextColumn Header="Кол-во после" Binding="{Binding ComponentName}" Width="100*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
